# エグゼクティブレポート — Trustworthy Backtest Framework

**日付:** 2026年6月10日
**対象:** `backtest-framework`(クオンツリサーチ・ポートフォリオプロジェクト)
**ステータス:** ✅ 健全 — 全67テストがパス(22秒)、作業ツリーはクリーン

## 1. プロジェクト概要

本プロジェクトは「この戦略は優れているか?」ではなく **「このバックテストは信頼できるか?」** という問いに答えるために設計された、Python製のバックテスト検証フレームワークです。搭載されている戦略(移動平均クロス)は意図的に平凡なものであり、**成果物は戦略ではなく検証の仕組みそのもの**です。クオンツ採用プロセスを想定したポートフォリオ作品として、コードベースを意図的に小さく保ち(実装約1,880行・テスト約860行)、すべての保証が数分で読んで検証できる規模に収められています。

## 2. 中核となる価値提案 — 「3つの柱」

バックテストが嘘をつく3つの典型的な方法それぞれに対し、具体的かつテスト済みの防御を実装しています。

| 柱 | 防ぐ「嘘」 | 防御の仕組み |
|---|---|---|
| **① ルックアヘッドバイアス** | 取引時点で知り得ない情報の使用 | エンジン内の1箇所(`EXECUTION_LAG = 1`)で構造的に排除。さらに `assert_causal()` が「未来を変えても過去のシグナルが変わらない」ことを能動的に証明 |
| **② 統計的有意性** | 運をスキルと誤認 | 並べ替え検定(p値)、ブートストラップ信頼区間、多重検定補正(Bonferroni/Šidák/Reality Check)。全関数がシード指定で完全再現可能 |
| **③ オーバーフィッティング** | 評価データへの過剰適合 | ウォークフォワード分析(anchored/rolling)でイン/アウトオブサンプルの劣化度を直接測定 |

特筆すべきは、ルックアヘッド検出テストが**わざと仕込んだ未来参照戦略(`_PerfectForesight`)を実際に捕まえることを検証**している点です。つまり「テストが本当に機能する」ことまでテストされています。

## 3. 品質状況

- **テスト:** 67件すべてパス。有意性・ウォークフォワードのテストは「答えが既知の合成データ」で検証され、ネットワークに依存しません。
- **誠実なデモ:** 実SPYデータでのノートブックは、戦略が**ベンチマークに全指標で負ける**結果と「エッジは運と区別不能」というデータ駆動の結論を隠さず提示。これ自体がプロジェクトの誠実性の実証になっています。
- **ドキュメント:** 英日両言語のREADME完備。アニュアライズ係数など全定数に根拠コメントあり。
- **ツーリング:** black / ruff によるフォーマット・lint体制。

## 4. 既知の制約(README内で明示済み)

サバイバーシップバイアス非対応、単一資産のみ、終値約定モデル、線形コストモデル、Deflated Sharpe Ratio未実装、グリッド選択ベースの最適化のみ — これらは隠さず「制約」として文書化されており、プロジェクトの誠実性方針と一貫しています。

## 5. 評価と提言

**強み:** 主張(構造的なバイアス排除)とそれを裏付けるテストが1対1で対応しており、ポートフォリオ作品として極めて説得力が高い。「負ける結果を正直に示す」デモは差別化要因です。

**今後の候補(優先度順):**

1. **Deflated Sharpe Ratio の実装** — READMEで未実装と明言している唯一の統計手法であり、柱②を完成させる
2. **CI(GitHub Actions)の導入** — 「全テストがパスする」という主張をバッジで常時証明できる
3. **マルチアセット対応** — 機能面で最大の拡張だが、現在の「小さく検証可能」という設計思想とのトレードオフを要検討

**総合判定:** プロジェクトは目的(信頼できるバックテストの実証)を十分に達成しており、コミット2件という若さながら完成度の高い状態にあります。
